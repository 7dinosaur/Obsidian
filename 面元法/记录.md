#
lower条件下所有面板都是一阶：
对应`mu_dim = 3, M_dim = 3, sigma_dim = 1, S_dim = 1`

`v_pert_c = matmul(this%A_g_to_c, v - this%v_inf)`扰动速度计算
`A_g_to_c`：坐标变换矩阵，无对尾迹的特殊处理

$C_{p,lin}=-\frac{2u'}{U_\infty}$与扰动速度是线性关系
$j_g=[0, 1, 0]$
`c_hat_g`：来流单位方向（来流速度/速度大小）
`A_g_to_c`：来流无攻角情况下为单位矩阵
压力系数排查完毕->dv计算错误
#
排查到`dv(1) = mu_params(2)`
但是有没有可能是`mu_params(2)`经过了坐标变换后才产生的错误呢？共同作用？
先看看坐标变换矩阵，没有问题再回来查`mu_params = get_doublet_parameters`
#
`v0`：与流向和面板法向正交的单位向量（局部展向/弦向）
`u0`：流向在面板上的投影，如果流向与面板法向正交即为流向
`B`：$\sqrt{M^2-1}$
$$C\_mat\_g=\left[\begin{array}{c}
1&0&0 \\0&1-M^2&0 \\0&0&1-M^2 \\
\end{array}\right]$$
$$B\_mat\_g=\left[\begin{array}{c}
1-M^2&0&0 \\0&1&0 \\0&0&1 \\
\end{array}\right]$$
`A_g_to_ls(1,:) = y * C_mat_g * u0`
`A_g_to_ls(2,:) = rs * B *C_mat_g * v0`
`A_g_to_ls(3,:) = B * y * n_g`
#
坐标变换矩阵看晕了，不过看起来没啥问题，先看mu_params吧。。。
`mu_params=T_mu * mu_vert` T_mu矩阵乘三个角的mu值
$$T_{mu} = S_{mu}^{-1}=\left[\begin{array}{c}
1 & x_1 & y_1\\
1 & x_2 & y_2\\
1 & x_3 & y_3\\
\end{array}\right]^{-1}$$
$x,y$ 均为面板的局部坐标，顶点坐标-中心坐标与A_g_to_ls相乘
